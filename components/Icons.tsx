import { AlertTriangle, Loader2, AlertCircle, Check } from "lucide-react";

export const Icons = {
    spinner: Loader2,
    warning: AlertCircle,
    alertTriangle: AlertTriangle,
    check: Check,
}; 